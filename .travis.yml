stages:
  - code-quality
  - testing
  - build
  - deploy

jobs:
  include:
    - stage: code-quality
      language: node_js
      node_js:
        - "10"
      cache:
        directories:
          - $HOME/.npm
      before_script:
        - npm install
      script:
        - npm run lint
